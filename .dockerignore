# 빌드 컨텍스트에서 제외 (이미지 크기/빌드 속도)
.git
.github
.gradle
.idea
build
# entrypoint.sh 는 이미지에 필요하므로 docker/ 전체를 제외하면 안 됩니다.
docker/mysql
*.md
docker-compose.yml
.env
.env.*
!.env.example
Dockerfile
.dockerignore

# 설정 파일은 컨테이너 시작 시 entrypoint 가 생성합니다.
# 실수로 이미지에 구워지지 않도록 제외합니다.
application-secret.properties
